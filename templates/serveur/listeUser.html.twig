{% extends 'base.html.twig' %}

{% block title %}liste User{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    h1 {}
</style>

<br />
<center><h2>Liste des Utilisateurs Enregistrés :</h2></center>
<br />

<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Nom</th>
      <th scope="col">Prénom</th>
	   <th scope="col">email</th>     
	   <th scope="col">Suppression</th>     
    </tr>
  </thead>
  <tbody>
	{% for item in listeUser %}
    <tr class="table-active">
      <th scope="row">{{ item.nom }}</th>
      <td>{{ item.prenom }}</td>
      <td>{{ item.email }}</td>
      <td><a href="{{ path('utilisateur_delete', {'id':item.id}) }}"><button type="button" class="btn btn-primary">Supprimer</button></a></p></td>
    </tr>
	{% endfor %}
  </tbody>
  <!-- fin affichage d'une liste -->
  
</table>

{% endblock %}